// alerta ao usuário
// alert('Olá, usuário')

// confirmação de operação
// confirm('Deseja finalizar a operação?')

// pedir dados ao usuário
// prompt('Digite um valor qualquer')

const primeiroValor = Number(prompt('Digite o primeiro valor'));
const segundoValor = Number(prompt('Digite o segundo valor'));
const resultado = primeiroValor + segundoValor;

// exibindo resultado
alert(`${primeiroValor} + ${segundoValor} = ${resultado}`)

// 'alert', 'confirm' e 'prompt' são métodos do objeto window
// window.alert('oi')